# ── Ink / system-Python parity (no venv) ──
# Install on the Pi, same interpreter as systemd ExecStart (/usr/bin/python3):
#
#   sudo apt-get install python3-pip python3-dev gcc
#   sudo python3 -m pip install --break-system-packages -r requirements.txt
#
# Or Debian packages where available: python3-flask python3-pil python3-qrcode ...

Flask>=3.0
Pillow>=10.0
qrcode[pil]>=7.4

# Waveshare epdconfig (older trees, ink-style): depends on SPI + bcm pins
spidev>=3.6
RPi.GPIO>=0.7.1

# Newer waveshare team epdconfig uses gpiozero (+ often lgpio on Bookworm).
# Not required if your ~/e-Paper .../epdconfig.py only imports RPi.GPIO + spidev.
# Uncomment if imports fail:
# gpiozero>=2.0
