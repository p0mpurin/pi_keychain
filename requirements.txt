# Web UI — on Raspberry Pi BEFORE pip install, run ./scripts/install_pi_deps.sh once
# or: sudo apt-get install -y swig python3-dev gcc liblgpio-dev
Flask>=3.0Pillow>=10.0
qrcode[pil]>=7.4

# ── Waveshare e-Paper (Raspberry Pi) — matches upstream epdconfig “RaspberryPi” class ──
# https://github.com/waveshareteam/e-Paper/blob/master/RaspberryPi_JetsonNano/python/lib/waveshare_epd/epdconfig.py
#
# Imports used when /proc/cpuinfo contains "Raspberry":
#   spidev   → /dev/spidev0.0
#   gpiozero → RST/DC/BUSY/PWR pins (LED/Button wrappers)
#
# Older Waveshare trees used RPi.GPIO — current master uses gpiozero, not RPi.GPIO.

spidev>=3.6
gpiozero>=2.0
# Prefer over RPi.GPIO on Pi OS Bookworm / kernel 6.x (avoids gpiozero.Button edge-detect crashes on BUSY).
# PyPI builds a native extension (`swig + liblgpio-dev`). If wheel build fails OR you omit build tools:
#   sudo apt install python3-lgpio && python3 -m venv --system-site-packages .venv
lgpio>=0.2; sys_platform == "linux"
